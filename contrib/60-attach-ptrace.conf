# Opt-in for attach: let your processes ptrace your other processes, so reptyr
# can steal them without extra privileges. Copy to /etc/sysctl.d/ and run
# `sysctl --system`. See README.md#ptrace-permission for the trade-offs.
kernel.yama.ptrace_scope = 0
